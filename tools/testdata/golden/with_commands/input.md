# Commands

Use `add-dirs` to load directories into the search pool.

`list-dirs` lists currently loaded directories while
`list-files` lists individual files.

Commands can appear mid-sentence: run `reload` after edits.
